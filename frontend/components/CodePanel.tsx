// Superseded by components/Panes.tsx, which splits this into EditorPane,
// ConsolePane, WantedPane and RunThisPane so the entry flow can show them as
// separate steps. Nothing imports this file — delete it.
export {};
